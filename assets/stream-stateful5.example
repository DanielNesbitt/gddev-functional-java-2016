public static void main(String[] args) {
    Stream.of(Debug.getStrings(1000))
        .map(s -> print("A"))
        .sorted()
        .limit(10)
        .map(s -> print("B"))
        .forEach(s -> {});
}
